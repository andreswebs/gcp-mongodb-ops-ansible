---
logging:
  receivers:
    mongodb:
      type: mongodb
  service:
    pipelines:
      mongo:
        receivers: [mongodb]
{# TODO: #}
{#
metrics:
  receivers:
    mongodb:
      type: mongodb
      {# insecure: true #}
      ca_file: {{ mongodb_tls_dir }}/{{ mongodb_file_ca_crt }}
      username: {{ mongodb_monitor_username }}
      password: {{ mongodb_monitor_password }}
      endpoint: {{ mongodb_monitor_endpoint }}
  service:
    pipelines:
      mongo:
        receivers: [mongodb]
#}
